/** OCR 测试连接超时（毫秒） */
export const OCR_TEST_TIMEOUT = 15_000

/** 模型连接测试超时（毫秒） */
export const MODEL_CONNECT_TIMEOUT = 30_000

/** 用户生成操作默认超时（毫秒） */
export const DEFAULT_GENERATION_TIMEOUT = 30_000

/** Worker 通信超时（毫秒） */
export const EMBEDDING_WORKER_TIMEOUT = 10_000
